import java.util.Scanner

fun main() {
    val scanner = Scanner(System.`in`)
    val input = scanner.next()
    val input2 = scanner.next()
    val input3 = scanner.next()
    val splittedInput = input3.
    print("$input\n$input2\n" + splittedInput.first() +" " + splittedInput.last())
}